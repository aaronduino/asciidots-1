#pragma once
#include "circuit.h"

// draw the complete circuit and components
void draw(Circuit circuit);

// draw the blank circuit map
void draw_circuit(Circuit circuit);

// colour tiles containing dots
void draw_dots(Circuit circuit);