DOT=$1

run()
{
  tput smcup # switch to alternate screen
  clear

  bin/dots $1

  tput rmcup # back to normal screen
}

if [ -e "$DOT" ] && [ ! -z "$DOT" ] # test the path
then
  run "$DOT"
else
  echo "do -h"
  # tell them to do -h
fi
